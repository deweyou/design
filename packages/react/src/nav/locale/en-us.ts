import type { NavLocaleText } from './types.ts';

const localeText = {
  more: 'More',
  moreNavigationItems: 'More navigation items',
  navigation: 'navigation',
  openNavigation: 'Open navigation',
} satisfies NavLocaleText;

export default localeText;
