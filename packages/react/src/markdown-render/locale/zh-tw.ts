import type { MarkdownRenderLocaleText } from './types.ts';

const localeText = {
  completedTask: '已完成任務',
  incompleteTask: '未完成任務',
} satisfies MarkdownRenderLocaleText;

export default localeText;
