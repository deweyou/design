import type { NavOverlayLocaleText } from './types.ts';

const localeText = {
  closeNavigation: 'Close navigation',
  navigation: 'Navigation',
} satisfies NavOverlayLocaleText;

export default localeText;
