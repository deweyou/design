# hooks

Reusable React hooks shared by components and app surfaces.

## Public Entrypoints

- `@deweyou-ui/hooks`: hook exports such as `useThemeMode`.

## Dependency Direction

- May depend on: React and `@deweyou-ui/utils`.
- Must not depend on: `components`, `website`, or `storybook`.
