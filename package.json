{
  "name": "deweyou-ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp run build -r && vp check && vp test",
    "dev": "vp run website#dev",
    "dev:website": "vp run website#dev",
    "dev:storybook": "vp run storybook#dev",
    "build": "vp run build -r",
    "test": "vp test",
    "check": "vp check",
    "prepare": "vp config",
    "release": "node scripts/release.mjs",
    "release:beta": "node scripts/release.mjs beta",
    "release:stable": "node scripts/release.mjs stable",
    "release:beta:dry": "node scripts/release.mjs beta --dry-run",
    "release:stable:dry": "node scripts/release.mjs stable --dry-run"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "changelogen": "^0.5.5",
    "typescript": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=24.14.0"
  },
  "packageManager": "pnpm@10.32.1"
}
